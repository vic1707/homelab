---
- name: features | internet | Configure forwarding | {{ firewall_zone.name | upper }}
  community.openwrt.uci:
      command: section
      config: firewall
      type: forwarding
      name: "{{ firewall_feature_sections[firewall_feature_name] | format(firewall_zone.name) }}"
      value:
          src: "{{ firewall_zone.name }}"
          dest: wan
      replace: true
      autocommit: true
  notify: Fedex | Reload firewall
