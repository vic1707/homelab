chain rescue_isolation_input {
	type filter hook input priority -1; policy accept;
	iifname != "eth1" ip daddr 192.168.255.0/24 drop
}

chain rescue_isolation_forward {
	type filter hook forward priority -1; policy accept;
	iifname "eth1" drop
	oifname "eth1" drop
}
